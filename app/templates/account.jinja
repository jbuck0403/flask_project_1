{% extends "base.jinja" %}
{% block title %}
  Account
{% endblock title %}
{% block content %}
  <div class="app-container">
    <div>
      <div class="welcome-wrapper">
        <div>Hello, {{ current_user.userName }}!</div>
        <div class="input-form-wrapper">
          <form method="post">
            {{ form.changeUserName.label }}
            {{ form.changeUserName }}
          </form>
          <form method="post">
            {{ form.logoutBtn(class="btn logout-btn") }}
          </form>
        </div>
      </div>
    </div>
  </div>
{% endblock content %}
